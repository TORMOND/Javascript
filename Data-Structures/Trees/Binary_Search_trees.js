class Node{
    constructor(){
        this.value = value
        this.left =null
        this.right =null
    }
}

class BST{
    constructor(){
        this.root = new Node(value)
        this.count = 0
    }
    size(){
     return this.count
    }
    insert(value){
     let newNode = new Node(value)
     this.count++
     console.log(`Added ${newNode} & there are ${this.count} items in the list`)
    const searchTree =node=>{
        //if value  < node.value, go left
      
        //if value  > node.value, go right

    }
    }
    min(){
     
    }
    max(){

    }
    contains(){

    }

    //Depth Search


    //Breadth Search
    
}

